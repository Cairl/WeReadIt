### 修复

- **兑换奖励 Token 过期告警**: Token 过期时任务以失败状态退出，推送消息明确提示"重新抓包更新 Token"，不再被静默吞掉或以成功状态推送。
- **Secret 旧名兼容**: 推送渠道 Secret 新名（PUSHPLUS / WXPUSHER / SERVERCHAN）优先，旧名仍可读取，升级后推送不再因 Secret 名不匹配而静默失效。

### 优化

- **推送重试次数**: 推送最大重试次数从 5 降到 3，避免长时间阻塞任务。
